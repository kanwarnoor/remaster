version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npx prisma generate
    build:
      commands:
        - env | grep -e DATABASE_URL >> .env.production
        - env | grep -e DIRECT_URL >> .env.production
        - env | grep -e JWT_SECRET >> .env.production
        - env | grep -e MONGO_URI >> .env.production
        - env | grep -e PUBLIC_URL >> .env.production
        - env | grep -e AWS_ACCESS_KEY_ID >> .env.production
        - env | grep -e AWS_SECRET_ACCESS_KEY >> .env.production
        - env | grep -e AWS_REGION >> .env.production
        - env | grep -e AWS_BUCKET_NAME >> .env.production
        - env | grep -e RAZORPAY_KEY_ID >> .env.production
        - env | grep -e RAZORPAY_KEY_SECRET >> .env.production
        - env | grep -e NODE_ENV >> .env.production
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
